/* exported defaults */

/* function defaults(target, source) {
  var finalObj = {};
  Object.assign(finalObj, target);
  for (var properties in target) {
    for (var keys in source) {
      if (properties !== keys) {
        finalObj[keys] = source[keys];
      }
    }
  }
  return finalObj;
}
*/
