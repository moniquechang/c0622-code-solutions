/* exported getLastChar */

/* function declaration with parameter: string (a string)
   find length of string (parameter) and subtract one to find index/position of
     last character of the string, and return the value of that last character */

function getLastChar(string) {
  return string[string.length - 1];
}
