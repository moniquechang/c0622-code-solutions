/* exported last */

/* function declaration with parameter: array (an array)
   find length of array and subtract 1 to get index num of last element of array,
     and return from function
*/

function last(array) {
  return array[array.length - 1];
}
