/* exported head */

/* function declaration with parameter: array (an array)
   first element of array is value at index number = 0 so,
     return array at [0]
*/

function head(array) {
  return array[0];
}
